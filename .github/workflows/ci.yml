name: CI

on:
  pull_request:
  push:
    branches: [main]

jobs:
  smoke:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Repo layout check
        run: |
          test -d frontend
          test -d backend
          test -d infra
          echo "ok"
